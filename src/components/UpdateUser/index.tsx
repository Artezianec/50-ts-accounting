import React from 'react';

const UpdateUser = () => {
    return (
        <>
            <button onClick={() => {

            }}>Change password</button>

            <button onClick={() => {

            }}>Edit User Profile</button>
        </>
    );
};

export default UpdateUser;